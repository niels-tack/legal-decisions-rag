
services:
  query-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - CASES_DB_PATH=/app/cases.db
      - ALLOWED_ORIGIN=http://localhost:5173
    volumes:
      - .:/app
    command: uv run uvicorn src.query_service.main:app --host 0.0.0.0 --port 8080
